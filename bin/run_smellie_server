#!/usr/bin/env python

from smellie_server import SmellieServer
from config import PORT

try:
    s = SmellieServer(PORT)
    s.serve_forever()

except KeyboardInterrupt as e: 
    print "\nKeyboard Interrupt, server closed "

    
